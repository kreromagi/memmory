{
  "name": "figma-ds-extractor",
  "version": "1.0.0",
  "description": "Figma Design System Extractor — extracts design tokens, components, and guidelines into Supabase memory",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site, no build needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
